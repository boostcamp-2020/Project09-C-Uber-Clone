directive @isAuthorized on FIELD_DEFINITION

type User {
  id: ID!
  email: String!
  name: String!
  password: String!
  phoneNumber: String!
  carType: String
  plateNumber: String
  description: String
  profileImage: String
}

type Query {
  user: User @isAuthorized
}